@*@using Before.Infrastructure.Extensions
@using Microsoft.Web.Mvc.Html*@
@{
    ViewBag.Title = "Home Page";
}
@if (Request.IsAuthenticated)
{
    //var customerId = User.Identity.GetCustomerId();
    <div class="jumbotron">
        @*@Html.ActionLink("Customer", "Single", "Customer", routeValues: new {id = customerId}, htmlAttributes: new {@class = "btn btn-default"})*@
    </div>
    <div class="partialContents" data-url="/dashboard/deviceslarge">
        <img src="~/Images/indicator.white.gif" /> Loading ...
    </div>
}
else
{
    <div class="jumbotron">
        <div>You need to @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" }) to have any fun!</div>
    </div>
}
