@*
        @using (Html.BeginForm("UploadFile", "FileUpload", FormMethod.Post, new { enctype = "multipart/form-data", data_ajax = "false" }))
        {
            @Html.AntiForgeryToken()
            @Html.TextBoxFor(model => model.Title, new { @class = "form-control" })
            @Html.TextAreaFor(model => model.Description, new { @class = "form-control", @rows = "7" })
        <input type="file" name="fileField" class="form-control filestyle">
        <input type="submit" value="Submit" class="btn btn-block" />
    }
*@